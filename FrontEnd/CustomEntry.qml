import QtQuick 2.15
import QtQuick.Window 2.15
import QtQuick.Controls 2.15

Rectangle {
    /*Behavior on color {
        SequentialAnimation {
            loops: 1
            ColorAnimation { from: "white"; to: "red"; duration: 1000 }
            ColorAnimation { from: "red"; to: "white";  duration: 1000 }
         }
    }

    Connections {
        target: dataSource
        onValueChanged: {
        reddot.color = "red"
        reddot.color = "#4d4d4d"
       }
    }*/
}
